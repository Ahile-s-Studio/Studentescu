@model Studentescu.ViewModels.GroupFeedViewModel
@{
    ViewBag.Title = "title";
    Layout = "_Layout";
}

<div>
    <div>
        <img src="@Model.Group.GroupImageUrl" alt="Group profile picture"/>
        <h3>@Model.Group.Name</h3>
    </div>
    <h3>@Model.Group.Members.Count() members</h3>
    @if (@Model.IsJoined)
    {
        <button>Leave</button>
    }
    else
    {
        <button>Join</button>
    }
    @if (@Model.IsJoined)
    {
        <button>Create New Post</button>
    }
    
    @foreach (var post in @Model.Posts)
    {
        @Html.Partial("_PostCardPartial", post)
    }
    
</div>