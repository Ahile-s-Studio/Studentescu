@page
@model AccessDeniedModel
@{
    ViewData["Title"] = "Access Denied";
}

<div class="min-h-screen flex flex-col justify-center items-center bg-gray-900 text-gray-200">
    <header class="text-center">
        <h1 class="text-4xl font-bold text-red-500 mb-4">
            @ViewData["Title"]
        </h1>
        <p class="text-lg text-gray-300">
            You do not have access to this resource.
        </p>
    </header>

    <div class="mt-6">
        <a href="/" class="inline-block px-6 py-3 text-sm font-medium bg-blue-600 text-white rounded-md hover:bg-blue-700 transition-all">
            Return to Home
        </a>
    </div>
</div>
